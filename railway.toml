[build]
builder = "DOCKERFILE"
dockerfilePath = "crm_agent/Dockerfile"
dockerContext = "crm_agent"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
