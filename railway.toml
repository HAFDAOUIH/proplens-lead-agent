[build]
builder = "DOCKERFILE"
dockerfilePath = "crm_agent/Dockerfile"
# dockerContext removed - Railway builds from repo root

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
